# 文件创建要求
- 每项单独的工具可能由多个文件构成, 在当前目录创建一个子文件夹, 文件夹名为工具名
- 如果工具只需要一个类或单个文件构成, 则文件名也使用工具名. 其他情况允许自行定义

# 测试要求
- 构建系统采用cmake, 放在工具目录中
- 测试程序命名为test.cpp, 放在工具目录中
- 在工具目录中创建build目录, 用于构建和测试. build目录中的所有内容被git忽略, 包括build/.gitignore文件自身
